# Decision → 导航：写入完整电子围栏快照（开机一次 + 云平台更新时）
#
# 实现：m_common/srv/SetGeoFence.srv
# Service：/mission_bridge/set_geofence（zone_manager 节点）
# 围栏：m_common/msg/GeoFence（fence_id / type=exclusion·inclusion / shape=polygon·circle / points / center / radius_m）
#
# 语义：全量替换；空数组 = 清除全部围栏；校验失败 success=false 且保留旧围栏
# 本地持久化（workspace_nav/json/nav_zones.json），重启自动恢复
# 详见 docs/作业区与电子围栏.md

m_common/GeoFence[] fences
---
bool success
string message
