# 仿真运行与联调（wuxihik_navigation / YILDIZ-USV）

本文是 **本仓库** 的主入口：Gazebo、EKF 定位、Nav2、**`converter`**、换图与调试命令。

**实船**（MAVROS、PX4、`gnss_odom_map_tf`、专用 Nav2 配置）在 **`USV_NAV`** 仓库维护，请勿在本仓找已删除的 `real_boat_*` / `nav2_real_mavros` launch。

**扩展阅读**

| 文档 | 内容 |
|------|------|
| [`地图与GNSS-Nav2对齐说明.md`](./地图与GNSS-Nav2对齐说明.md) | 栅格、`ref_gnss*`、datum、仿真下 EKF/navsat |
| [`map与gnss的对齐.md`](./map与gnss的对齐.md) | 长文：地图与 GNSS 几何关系 |
| [`../src/YILDIZ-USV/docs/PROJECT_ARCHITECTURE_AND_NAV2.md`](../src/YILDIZ-USV/docs/PROJECT_ARCHITECTURE_AND_NAV2.md) | 包分工与数据流 |

---

## 运行前环境

每个新终端：

```bash
source /opt/ros/humble/setup.bash
source <工作区>/install/setup.bash
```

---

## 定位逻辑（仿真默认）

| 问题 | 答案 |
|------|------|
| 用包内 EKF 吗？ | **用。** `localization.launch.py` 启动 EKF +（可选）navsat。 |
| 里程计给 Nav2 | **`/odometry/filtered`**（与 `nav2_params.yaml` 一致）。 |
| GPS / IMU 源 | 默认 **`/roboboat/sensors/gps/navsat`**、**`/roboboat/sensors/imu/imu`**（与 Gazebo 桥一致，可在 launch 改 `gps_src` / `imu_src`）。 |
| `use_sim_time` | Gazebo 运行时 **true**（三处 launch 均建议显式传入）。 |

```text
Gazebo ──► 传感器 ──► covariance repub ──► EKF (+ navsat) ──► /odometry/filtered ──► Nav2
```

---

## 三终端启动（仿真）

```
终端1 ── Gazebo + bridge
终端2 ── localization（EKF）
终端3 ── Nav2 + RViz
终端4 ── （可选）converter、航点、手测
```

| 步骤 | 命令 |
|:---:|------|
| **1** | `ros2 launch workspace_gz simulation.launch.py` |
| **2** | `ros2 launch workspace_ros localization.launch.py use_sim_time:=true` |
| **3** | `ros2 launch workspace_nav nav2.launch.py use_sim_time:=true enable_mission_bridge:=true`（地面站链路见下） |

地面站 **`/waypoint`**、**`/color_code`**：

- **`enable_mission_bridge:=true`**：`mission_bridge` 与 Nav2 **同地图**装订，写 **`waypoints.json` / `target_buoy.json`** 并调 **FollowWaypoints**（与 USV_NAV 载荷格式一致）。
- 仅需要做文件落盘时用：`ros2 run workspace_nav waypoint_transform`（不写盘仅调试用可分立 `waypoint_with_state`，勿与 bridge 并行重复）。

默认海图：**`workspace_nav/config/map.yaml`**。换图：

```bash
ros2 launch workspace_nav nav2.launch.py use_sim_time:=true map:=/绝对路径/你的图.yaml
```

---

## Nav2 → 仿真推进器

仿真 **不要** 使用实船的 MAVROS 速度桥。使用包内 **`converter`**：

```bash
ros2 run workspace_ros converter
```

Nav2 控制器发布 **`/cmd_vel_nav`**（见 `nav2_params.yaml` 与 bringup remap）；`converter` 将其转为 Gazebo 推进器话题。键盘手控可用 **`manual_control`**（见包说明）。

---

## 航点与地面站

```bash
ros2 run workspace_nav waypoint_transform
ros2 run workspace_nav waypoint_with_state
```

分立节点与 **`mission_bridge` 勿重复运行**（会争用同一 `/waypoint` 与 FollowWaypoints）。

- **`waypoint_transform`**：单次任务写 **`waypoints.json`**；默认 **`datum_source:=map_yaml`**（与 **`map.yaml`** 的 `ref_gnss*`）；`datum_source:=first_gps` 时用仿真 GPS 话题（默认 **`/roboboat/sensors/gps/navsat`**）。
- **`waypoint_with_state`**：默认 **`/odometry/filtered`**，从 **`waypoints.json`** 跟进 Nav2。

---

## 调试命令速查

```bash
ros2 topic echo /odometry/filtered --once
ros2 topic echo /clock --once
ros2 run tf2_ros tf2_echo map base_link
ros2 topic list | grep roboboat
```

需要 MAVROS/PX4 与 Nav2 的专门参数文件时，请使用 **USV_NAV**。

---

## ROS_DOMAIN_ID 与网络

与实船、NX 同网时，用不同 **`ROS_DOMAIN_ID`** 或独立网络，避免话题串线。

---

## 实船文档哪里看

克隆并阅读 **`USV_NAV`** 仓库中的 `docs/项目运行与联调.md` 与 `src/USV_NAV/docs/*`。
